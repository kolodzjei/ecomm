<%= form_with url: items_path, scope: :item do |form| %>
        <%= form.hidden_field :product_id, value: @product.id %>
        <%= form.hidden_field :cart_id, value: current_user.cart.id %>
        <%= form.submit "Add to cart", class: "btn btn-primary" %>
<% end %>