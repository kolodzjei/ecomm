<%= form_with url: items_path, scope: :item do |f| %>

<%= f.hidden_field :product_id, value: @product.id %>
<%= f.hidden_field :cart_id, value: current_user.cart.id %>

<%= f.submit "Add to cart", class: "btn btn-success" %>
<% end %>